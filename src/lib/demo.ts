/**
 * 演示模式预置回复
 * 当用户未配置 API Key 时，提供示例内容让评审可以体验产品
 */

export const DEMO_TOPICS: Record<string, string> = {
  递归: `### 1. 前置概念

在理解递归之前，你需要先知道什么是**函数**：函数是一段可以重复执行的代码，可以接收输入（参数）并返回输出。

还需要知道**调用栈**的概念：当函数 A 调用函数 B 时，程序会"暂停" A，先执行 B，B 执行完后再回到 A 继续执行。这个"暂停-恢复"的机制就是调用栈。

### 2. 核心定义

**递归**是一种编程技巧：一个函数在它的函数体内**调用它自己**。

就像照镜子时镜子里还有镜子，递归就是"自己调用自己"。但为了避免无限循环，递归必须有一个**终止条件**（也叫基线条件），当满足条件时停止调用自己。

### 3. 为什么重要

递归是计算机科学的核心概念之一。很多问题天然适合用递归解决：
- 树和图的遍历（如文件系统目录结构）
- 分治算法（如归并排序、快速排序）
- 回溯问题（如走迷宫、八皇后）

掌握递归后，你会获得一种新的思维方式：把大问题拆成"同类型的小问题"。

### 4. 学习问法

常见考点：
- 给一个场景，要求写出递归函数
- 分析递归函数的执行结果
- 递归转迭代
- 计算递归的时间复杂度和空间复杂度

### 5. 答题模板

写递归函数的三步模板：

1. **找终止条件**：什么情况下不再递归？
2. **找递归关系**：大问题如何拆成小问题？
3. **返回结果**：合并小问题的结果

示例（计算阶乘）：

\`\`\`
function factorial(n) {
  // 1. 终止条件
  if (n <= 1) return 1
  // 2. 递归关系：n! = n × (n-1)!
  // 3. 返回结果
  return n * factorial(n - 1)
}
\`\`\`

### 6. 易错点

- **忘记终止条件**：导致栈溢出（Stack Overflow），程序崩溃。
- **终止条件写错**：比如写成 \`if (n === 0)\` 但传入负数，依然无限递归。
- **递归方向错误**：应该向终止条件靠近，比如 \`factorial(n)\` 调用 \`factorial(n-1)\`，而不是 \`factorial(n+1)\`。
- **重复计算**：如斐波那契数列的朴素递归会重复计算大量子问题，需要用记忆化优化。

### 7. 小练习

请回答：如果调用 \`factorial(3)\`，函数的执行过程是怎样的？最终返回什么？

提示：按照"调用栈"的思路，写出每一步调用了什么，返回了什么。`,

  'HTTP 协议': `### 1. 前置概念

在理解 HTTP 之前，你需要知道什么是**网络通信**：两台计算机之间通过网络传输数据。

还需要知道**客户端**和**服务器**的概念：
- **客户端**：发起请求的一方（如你的浏览器）
- **服务器**：接收请求并返回响应的一方（如网站的后端）

### 2. 核心定义

**HTTP**（HyperText Transfer Protocol，超文本传输协议）是客户端和服务器之间通信的"语言规则"。

它采用**请求-响应**模型：
1. 客户端发送一个 HTTP 请求
2. 服务器接收请求，处理后返回一个 HTTP 响应

就像去餐厅点餐：你（客户端）向服务员（网络）说"我要一份炒饭"（请求），厨房（服务器）做好后端给你（响应）。

### 3. 为什么重要

HTTP 是整个 Web 的基础协议。你访问的每一个网页、调用的每一个 API，底层几乎都是 HTTP。理解 HTTP 是前端开发、后端开发、爬虫、网络安全等领域的必备知识。

### 4. 学习问法

常见考点：
- HTTP 请求方法有哪些？区别是什么？
- HTTP 状态码的含义（200、404、500 等）
- GET 和 POST 的区别
- HTTP 和 HTTPS 的区别
- HTTP 报文的结构

### 5. 答题模板

HTTP 请求报文结构：

\`\`\`
方法 路径 HTTP/1.1        ← 请求行
Host: 域名                 ← 请求头
Content-Type: 类型

请求体（可选）              ← 请求体
\`\`\`

HTTP 响应报文结构：

\`\`\`
HTTP/1.1 状态码 状态描述    ← 状态行
Content-Type: 类型          ← 响应头

响应体                      ← 响应体
\`\`\`

常见状态码：
- 2xx 成功（200 OK）
- 3xx 重定向（301 永久重定向）
- 4xx 客户端错误（404 未找到）
- 5xx 服务器错误（500 内部错误）

### 6. 易错点

- **混淆 GET 和 POST**：GET 参数在 URL 里，POST 参数在请求体里。GET 会被浏览器缓存，POST 不会。
- **以为 HTTP 是安全的**：HTTP 是明文传输，任何人都能截获内容。HTTPS 才是加密的。
- **状态码记混**：404 是"资源不存在"，500 是"服务器内部错误"，不要搞反。
- **以为 HTTP 是有状态的**：HTTP 本身是无状态协议，每次请求都是独立的。状态管理靠 Cookie/Session/Token。

### 7. 小练习

请回答：当你在浏览器地址栏输入 \`https://example.com\` 并回车，浏览器发送的 HTTP 请求大概长什么样？写出请求行和至少两个请求头。`,

  光合作用: `### 1. 前置概念

在理解光合作用之前，你需要先知道几个基础概念：

- **细胞**：生物体的基本结构和功能单位。植物细胞里有叶绿体。
- **叶绿体**：植物细胞里进行光合作用的"工厂"，里面有叶绿素。
- **叶绿素**：一种绿色色素，能吸收太阳光的能量。

### 2. 核心定义

**光合作用**是植物利用**光能**，把**二氧化碳和水**转化成**有机物（葡萄糖）和氧气**的过程。

用一句话说：植物"吃"阳光 + 水 + 二氧化碳，"产出"养分 + 氧气。

文字表达式：

\`\`\`
二氧化碳 + 水  —(光能、叶绿体)→  有机物（葡萄糖） + 氧气
\`\`\`

### 3. 为什么重要

光合作用是地球上最重要的生物化学反应之一：
- 它是**几乎所有生命的能量来源**：植物通过光合作用制造有机物，草食动物吃植物，肉食动物吃草食动物，能量就这样流动。
- 它**维持大气中的氧气平衡**：我们呼吸的氧气，绝大多数来自植物的光合作用。
- 它**影响全球气候**：光合作用吸收二氧化碳，是碳循环的关键环节。

### 4. 学习问法

常见考点：
- 光合作用的条件、原料、产物是什么？
- 光合作用的文字表达式 / 化学方程式
- 光合作用和呼吸作用的区别
- 影响光合作用速率的因素（光照强度、二氧化碳浓度、温度）

### 5. 答题模板

光合作用必背四要素：

| 要素 | 内容 |
|------|------|
| 条件 | 光能、叶绿体 |
| 原料 | 二氧化碳、水 |
| 产物 | 有机物（葡萄糖）、氧气 |
| 场所 | 叶绿体 |

化学方程式（初中了解即可）：

\`\`\`
6CO₂ + 6H₂O —(光能、叶绿体)→ C₆H₁₂O₆ + 6O₂
\`\`\`

### 6. 易错点

- **把原料和产物搞混**：二氧化碳和水是原料（输入），有机物和氧气是产物（输出）。
- **忘记叶绿体是条件**：光合作用必须在叶绿体里进行，没有叶绿体的细胞（如根部细胞）不能进行光合作用。
- **以为植物只进行光合作用**：植物也会进行呼吸作用，消耗有机物。白天光合作用 > 呼吸作用，夜晚只有呼吸作用。
- **以为光合作用只在叶片进行**：任何有叶绿体的绿色部分都能进行光合作用，如幼嫩的绿色茎。

### 7. 小练习

请回答：如果把一株植物放在黑暗环境中几天，它会怎样？为什么？

提示：从光合作用的条件来思考。`,
}

/** 演示模式：根据用户输入匹配预置回复 */
export function getDemoResponse(userInput: string): string | null {
  const input = userInput.trim()

  // 精确匹配
  if (DEMO_TOPICS[input]) return DEMO_TOPICS[input]

  // 模糊匹配
  for (const [topic, response] of Object.entries(DEMO_TOPICS)) {
    if (input.includes(topic) || topic.includes(input)) {
      return response
    }
  }

  // 未匹配到，返回提示
  return `### 演示模式提示

当前处于**演示模式**，仅支持以下示例知识点的讲解：

- **递归** — 编程概念
- **HTTP 协议** — 网络基础
- **光合作用** — 生物基础

请尝试输入上述关键词之一来体验完整的知识点讲解功能。

> 💡 想要学习任意知识点？请到**设置**页配置你自己的 API Key（支持 OpenAI、DeepSeek 等），即可解锁完整的 AI 学习能力。`
}
